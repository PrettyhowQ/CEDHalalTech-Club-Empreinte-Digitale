# Configuration CED HalalTech™ - Template de variables d'environnement
# Copier ce fichier vers .env et configurer vos vraies valeurs

# === BASE DE DONNÉES ===
# PostgreSQL URL - Modifier selon votre configuration
DATABASE_URL=postgresql://username:password@localhost:5432/ced_halaltech

# === SERVICES IA ===
# Clé API Anthropic Claude (requise pour Super IARP Pro)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Clé API OpenAI (optionnelle, pour fonctionnalités alternatives)
OPENAI_API_KEY=sk-proj-your-openai-key-here

# === APPLICATION ===
# Environnement (development, production)
NODE_ENV=development

# Port du serveur (défaut: 5000)
PORT=5000

# === SÉCURITÉ ===
# Secret JWT pour tokens (générer une clé sécurisée en production)
JWT_SECRET=your-super-secret-jwt-key-here

# Clé de chiffrement (32 caractères minimum)
ENCRYPTION_KEY=your-32-character-encryption-key

# === REPLIT (si déployé sur Replit) ===
# URL base de données Replit
REPLIT_DB_URL=your-replit-db-url

# Domain Replit
REPLIT_DOMAIN=your-app-name.replit.app

# === EMAIL (optionnel) ===
# Configuration SMTP pour notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password

# === STORAGE (optionnel) ===
# AWS S3 pour stockage fichiers
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_BUCKET_NAME=ced-halaltech-assets
AWS_REGION=eu-west-1

# === CDN (optionnel) ===
# Cloudinary pour images
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Cloudflare pour CDN
CLOUDFLARE_API_TOKEN=your-cloudflare-token
CLOUDFLARE_ZONE_ID=your-zone-id

# === MONITORING (optionnel) ===
# Sentry pour tracking erreurs
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project

# Google Analytics
ANALYTICS_ID=GA-XXXXXXXXX

# === REDIS (optionnel) ===
# URL Redis pour cache et sessions
REDIS_URL=redis://localhost:6379

# === API EXTERNES (optionnel) ===
# Stripe pour paiements
STRIPE_SECRET_KEY=sk_test_your_stripe_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# PayPal pour paiements alternatifs
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret

# === DÉVELOPPEMENT UNIQUEMENT ===
# Debug mode détaillé
DEBUG=ced:*

# Hot reload activé
HOT_RELOAD=true

# === PRODUCTION UNIQUEMENT ===
# Domaine de production
PRODUCTION_DOMAIN=ced-halaltech.com

# SSL certificates paths
SSL_CERT_PATH=/path/to/ssl/cert.pem
SSL_KEY_PATH=/path/to/ssl/private.key

# === NOTES IMPORTANTES ===
# 1. Ne jamais commiter le fichier .env dans git
# 2. Utiliser des clés sécurisées en production
# 3. Renouveler régulièrement les secrets
# 4. Activer 2FA sur tous les comptes de services
# 5. Chiffrer les sauvegardes de base de données